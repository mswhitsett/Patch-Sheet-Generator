[package]
name = "waymaker-patch-sheet"
version = "0.1.0"
description = "Waymaker Patch Sheet"
authors = ["Waymaker"]
license = ""
repository = ""
edition = "2021"
rust-version = "1.77.2"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
base64 = "0.22"

[features]
custom-protocol = ["tauri/custom-protocol"]
